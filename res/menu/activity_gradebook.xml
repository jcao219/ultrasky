<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <item
        android:id="@+id/menu_watched_settings"
        android:orderInCategory="100"
        android:showAsAction="never"
        android:title="@string/menu_watched_settings"
        android:onClick="onWatchedMenuItemClicked"
        android:enabled="false"/>
    
    <!-- button to trigger autocheck grades -->
    <item android:id="@+id/menu_check_now"
        android:title="@string/menu_check_now" 
        android:onClick="onCheckNowMenuItemClicked"/>

</menu>